// sparkle-cursor.js — DISABLED.
// The gold sparkle cursor-trail animation has been turned off.
// The cursor behaves as the normal system pointer with no trailing effect.
// (File kept so the <script> reference resolves and the effect can be
//  re-enabled later if desired.)
(function () {
  /* no-op: cursor animation intentionally removed */
})();
